{
  "name": "fizzbuzz-test",
  "version": "1.0.0",
  "description": "## Step 1: Write a function that returns the following result, for a contiguous range of numbers: * the number * 'fizz' for numbers that are multiples of 3 * 'buzz' for numbers that are multiples of 5 * 'fizzbuzz' for numbers that are multiples of 15",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha ./tests/**.spec.js"
  },
  "license": "ISC",
  "homepage": "https://github.com/tmerrr/fizzbuzz-test#readme",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4"
  }
}
